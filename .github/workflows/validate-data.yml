name: Validate question data

on:
  pull_request:
    paths:
      - 'data/questions/**'
  push:
    paths:
      - 'data/questions/**'

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node data/questions/validate.js
